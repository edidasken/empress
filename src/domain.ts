export type Status='held'|'confirmed'|'checked_in'|'in_service'|'completed'|'cancelled'|'expired';export type Entry={id:string;delta:number};const transitions:Record<Status,Status[]>={held:['confirmed','expired'],confirmed:['checked_in','cancelled'],checked_in:['in_service','cancelled'],in_service:['completed'],completed:[],cancelled:[],expired:[]};export function transition(a:Status,b:Status){if(!transitions[a].includes(b))throw Error('STATE_INVALID_TRANSITION');return b}export function overlaps(a:{start:number,end:number},b:{start:number,end:number}){return a.start<b.end&&b.start<a.end}export function balance(e:Entry[]){const seen=new Set;return e.reduce((s,x)=>seen.has(x.id)?s:(seen.add(x.id),s+x.delta),0)}export function sign<T>(value:T,time:string){return Object.freeze({status:'signed' as const,time,version:1,value:structuredClone(value)})}export function safeLog(o:Record<string,unknown>){const allow=new Set(['severity','eventName','timestamp','environment','service','buildId','correlationId','outcome','errorCode','tenantRef','resourceRef']);for(const k of Object.keys(o))if(!allow.has(k))throw Error('LOG_FIELD_REJECTED');return Object.freeze({...o})}
